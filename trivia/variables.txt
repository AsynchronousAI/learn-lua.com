How to define a type?
- variable = "hello": string
-* variable: string = "hello"
- variable.type(string) = "hello"
- string: variable = "hello"
Whats a variable?
- An instance that appears in the explorer
- A number that can be reused and changed
- A string that can be reused and changed
-* A item which can take many types and can be reused or changed
